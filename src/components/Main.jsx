const Main = ({ children }) => {
  return (
    <main className="container mx-auto flex min-h-screen items-center justify-center px-6 py-20 tracking-tight">
      {children}
    </main>
  );
};

export default Main;
